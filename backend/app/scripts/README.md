# Database Setup

Run these scripts in order and under the correct user. Replace placeholder
passwords.

1. Connect to `fioapi_db` as `postgres` user → run `db_setup01_postgres.sql`
2. Connect to `fioapi_db` as `postgres` user → run `db_setup02_fioapi_admin.sql`
